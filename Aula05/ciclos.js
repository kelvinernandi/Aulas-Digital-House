const frutas = [
    'laranja',
    'Melão',
    'Acerola'
]

for (let contador = 0; contador <= frutas.length; contador++) {
    console.log(`Acessando o elemento ${frutas[contador]}, no índice ${contador}`)
}